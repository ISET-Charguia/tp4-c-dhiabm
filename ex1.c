#include <stdio.h>
#include <stdlib.h>

int main()
{
int j,n,i,tab[n];
printf("donner la taille de tableau");
scanf("%d",&n);
for (i=0;i<=n;i++) {
    tab[i];}

    for (j=0;j<=n;i++) {
printf("%d\n", tab[i]);
    }

    return 0;
}
